{
  "name": "my_subgraph_slug",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --studio <DEPLOY_KEY>",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ <SUBGRAPH_SLUG>",
    "create-local": "graph create --node http://localhost:8020/ <SUBGRAPH_SLUG>",
    "remove-local": "graph remove --node http://localhost:8020/ <SUBGRAPH_SLUG>",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 <SUBGRAPH_SLUG>",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.2"
  }
}
